# Commando Configuration - Basic Example
#
# Demonstrates minimal Commando module setup with simple commands.

# Modules to load - commands auto-discovered from exports
modules:
  - ./simple    # Uses __module__ to rename group to "basic"

# Command-specific settings
settings:
  basic.greet:
    defaultName: Commando User
